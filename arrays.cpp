#include<bits/stdc++.h>
using namespace std;

int main() {
    // arrays
    // int arr[5];
    // cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];
    // arr [2] += 10;
    // cout << arr[2];

    // strings
    string s = "Bajpai";
    int len = s.size();
    s[len-1] = 'e';
    cout << s[len - 1];

    return 0;
}